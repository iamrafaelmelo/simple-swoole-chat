{
    "require": {
        "php": ">=8.1",
        "ext-swoole": "*"
    },
    "require-dev": {
        "swoole/ide-helper": "^5.0"
    },
    "scripts": {
        "start": [
            "Composer\\Config::disableProcessTimeout",
            "composer dump-autoload -o",
            "php bin/server.php"
        ]
    }
}
